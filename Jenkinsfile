pipeline {    
    agent any    
    stages {        
        stage ('build') {            
            steps {
                sh "echo Everything woking fine so nice." 
                sh "echo build"
            }            
        }
    }
}
