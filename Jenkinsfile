pipeline {    
    agent any    
    stages {        
        stage("Composer Init") {
            steps {                                
                sh "first stage (start)"
            }
        }
    }
}
